To test the script in UNIX command line, you may compile php as cgi binary and
install it to /usr/local/bin. In your script you need add a line at the top of
the script

#!/usr/local/bin/php

And also 

    phpshow();

should be changed to 

    phpshow('graph.gif');
